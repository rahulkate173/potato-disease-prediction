FROM tensorflow/serving:latest

COPY models /models

ENV MODEL_NAME=disease_models
ENV MODEL_BASE_PATH=/models

EXPOSE 8501

CMD ["tensorflow_model_server", "--rest_api_port=8501", "--model_name=disease_models", "--model_base_path=/models"]